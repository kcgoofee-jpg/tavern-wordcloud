/**
 * Feature flags. Implementations stay in place; only the UI entry points are gated.
 *
 * shareQr: QR-code share view and "copy share link" button.
 */
export const FEATURES = {
  shareQr: false,
} as const;
